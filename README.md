# SHAPe Avocado — Enterprise Streamlit Scaffold

## Run locally
```powershell
pip install -r requirements.txt
streamlit run app.py
```

## Data
Place `shape_data.xlsx` in the project root with sheets:
- Baseline
- Metrics (optional)

## Access control
Create `.streamlit/secrets.toml` (do not commit it). Use `.streamlit/secrets.toml.example` as a template.
